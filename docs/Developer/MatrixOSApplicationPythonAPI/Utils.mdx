---
title: Utils
sidebar_position: 9
---

# Utils API

`MatrixOS.Utils` exposes small helper functions.

Methods:

- `string_hash(text: str) -> int`

Normal NVS users do not need to call `string_hash()` manually. String keys are hashed by the binding.
