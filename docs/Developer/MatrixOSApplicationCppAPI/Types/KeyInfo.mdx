---
title: KeyInfo
unlisted: true
---

# KeyInfo

`KeyInfo` was the older keypad event data type. Current Matrix OS C++ apps should use [`KeypadInfo`](../Input) through `InputEvent::keypad`.

See [Migration From 3.x to 4.x](../MigrationFrom3To4) for the input API changes.
